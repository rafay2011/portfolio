import type { Testimonial } from "./types";

/**
 * Testimonials — placeholder structure, hidden until real ones are supplied.
 * Kept empty by design; nothing fabricated. The Testimonials section renders
 * an "available on request" state while this is empty.
 */
export const testimonials: Testimonial[] = [];
